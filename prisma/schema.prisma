
generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["driverAdapters"]
}

datasource db {
  provider = "sqlite"
  url      = "file:./prisma/schema.prisma"
}
model User {
  id    String @id  @default(uuid())
  email String  @unique
  name  String?
}
